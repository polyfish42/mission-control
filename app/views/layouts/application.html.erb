<!DOCTYPE html>
<html>
  <head>
    <title>MissionControl</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <script type="text/javascript">
      <% if logged_in? %>
        window.currentUser = <%= render(
          "api/users/user.json.jbuilder",
          user: current_user
        ).html_safe %>
      <% end %>
    </script>

    <script type="text/javascript">
      window.splash_img = "<%= asset_path("splash/splash_page.jpg") %>"
      window.logo = "<%= asset_path("splash/logo.png") %>"
      window.small_logo = "<%= asset_path("session/small-logo.png") %>"
      window.avatar = "<%= asset_path("project/avatar_placeholder.png") %>"
    </script>
    <%= yield %>
  </body>
</html>
